<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"> <title>Image Processing API!</title>
    <link rel="stylesheet" href="public/main.css">
  </head>
<body>
  <h1>Image Processing API</h1>
  <% if ( data.length > 0 ) { %>
    <div id="image-container">
      <h3>Image available!</h3>
      <% data.forEach((img) => { %>
        <img class='sized-image' src="assets/full/<%= img %>">
      <% }); %>
    </div>
  <% } else { %>
    <div class='noParams'>
      <h3>No image available!</h3>
      <p>Please provide a url as http://localhost:3000/resize?w=500&h=400</p>
    </div>
    
  <% } %>
  
</body>
</html>